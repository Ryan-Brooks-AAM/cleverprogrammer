import turtle

polygon = turtle.Turtle()
num_sides = [3,4,5,6,7,8,9,10,11]
side_length = 100

def poly_turtle():
        angle = 360.0 / s
        polygon.forward(side_length)
        polygon.right(angle)


for i in range(500):
    for s in num_sides:
        s = int(s)
        poly_turtle()

turtle.done()
